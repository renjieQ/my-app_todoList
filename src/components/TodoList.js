import React from "react";

const TodoList = ({ todos }) => {
  return (
    <div className="todo-container">
      <ul className="todo-list">what? i am TodoList</ul>
    </div>
  );
};

export default TodoList;
